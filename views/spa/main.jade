div(ng-controller="Main")
	nav.navbar.navbar-default.navbar-fixed-top
		div.container
			div.navbar-header
				button.navbar-toggle.collapsed(type="button", data-toggle="collapse", data-target="#navbar", aria-expanded="false", aria-controls="navbar")
					span.sr-only Toggle Navigation
					span.icon-bar
					span.icon-bar
					span.icon-bar
				a.navbar-brand(ui-sref="/login") Game Tournament
			div#navbar.navbar-collapse.collapse
				ul.nav.navbar-nav
					li.active
						a(ui-sref="index") Home
					li 
						a(ui-sref="about") About
					li
						a(ui-sref="groups") Groepen
				ul.nav.navbar-nav(ng-show="loggedIn")
					// li
					// 	a(ui-sref="test") Test
				ul.nav.navbar-nav.navbar-right
					li(ng-show="!loggedIn")
						a(ui-sref="auth/login") Login
					li(ng-show="!loggedIn")
						a(ui-sref="auth/register") Register
					li.dropdown-toggle(ng-show="loggedIn")
						a(data-toggle="dropdown", role="button", aria-haspopup="true", aria-expanded="false") 
							| {{ user.firstname + ' ' + user.lastname }}
							span.caret
						ul.dropdown-menu
							li
								a(ui-sref="profile") Profiel
							li
								a(ng-click="logout()") Logout 
				
	div.container(style="margin-top: 50px;")
		block content
block scripts