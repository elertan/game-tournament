extends ../main.jade

block content
	div(ng-controller="GamesShowController" style="color: #fff; text-shadow: -1px 0 black, 0 1px black, 1px 0 black, 0 -1px black;")
		div(ng-show="!gameNotFound")
			h1 {{ gameinfo.name }}
			p {{ gameinfo.description }}
		div(ng-show="gameNotFound")
			h1 Game was not found